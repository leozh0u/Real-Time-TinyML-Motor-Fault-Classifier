# firmware/Drivers/CMSIS-NN/

Vendored CMSIS-NN library (quantized int8 conv kernels) goes here once pulled in via STM32CubeIDE's software pack manager or as a git submodule. Not vendored yet. Only needed if the TFLite Micro runtime path is *not* chosen — see root README "Toolchain decisions."
