# data/processed/

Windowed feature tensors (FFT/spectrogram band energies per channel) derived from `data/raw/`, split into train/val/test stratified by remount ID (not just by class).

Populated by `training/data_prep/preprocess.py` (not yet implemented). Gitignored — regenerate from `data/raw/` rather than committing.
