# data/sample/

A small representative slice of the dataset (a few windows per class), committed to the repo so reviewers can see real data shape/format without pulling the full raw dataset. Not yet populated — fill in once `data/raw/` has real logged data.
