# benchmarks/plots/

Pareto/tradeoff frontier plots (accuracy vs latency, accuracy vs RAM) across the PTQ/QAT x model-size sweep, plus fusion ablation comparison charts. Generated from `benchmarks/results/` data — empty until the sweep runs.
